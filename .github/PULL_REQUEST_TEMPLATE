<!--
Thanks for your contribution! Please read this comment in its entirety. It's quite important.
When a contributor merges the pull request, the title and the description will be used to build the merge commit!

### Pull Request TITLE

It should follow the **Conventional Commits** format:
`type(scope): summary` (scope is optional but encouraged)

Example:
`feat(ui): [ 12345 ] Add support for custom themes`
`fix(core): [ 54321 ] Fix crash when loading corrupted TIFFs`
-->

### Linked issues
<!--
Link the Issue(s) this Pull Request is related to.

Each PR should link to at least one issue, in the form:

Use one line for each Issue. This allows auto-closing the related issue when the fix is merged.

Fixes #12345
Fixes #54345
-->

### Checklist

- [ ] I have read the [CONTRIBUTING.md](CONTRIBUTING.md) document.
- [ ] I have added unit tests for my changes.
- [ ] I have verified that all existing tests pass.
- [ ] My code follows the project's coding style (run `pre-commit run --all-files` locally).
- [ ] I have updated the documentation (if applicable).

### Summarize your change.

### Describe the reason for the change.

### Describe what you have tested and on which operating system.

### Add a list of changes, and note any that might need special attention during the review.

### If possible, provide screenshots.